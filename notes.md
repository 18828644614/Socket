# Notes: 字节、编码与数据边界

## Existing context
- 目标文档：`01-基础入门/字节、编码与数据边界.md`
- 相关文档：`02-TCP Socket/消息边界与拆包.md`、`02-TCP Socket/发送与接收数据.md`、`05-协议与工程实践/消息格式与版本.md`

## Findings
- 字节是网络 API 处理的基本单位；字符、Unicode 码点和编码后的字节序列必须分开讲解。
- UTF-8 是变长编码，中文常见为 3 字节，emoji 常见为 4 字节；字符数不能当作字节数。
- C 字符串依赖结尾 `\\0`，二进制网络数据必须显式携带长度。
- TCP 是没有消息边界的字节流，`send`/`recv` 都可能只完成一部分；UDP 保留数据报边界但不保证可靠性。
- 分帧可用固定长度、分隔符、长度前缀或自描述格式；长度前缀需要网络字节序、最大长度和溢出检查。
