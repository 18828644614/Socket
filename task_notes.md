# Notes: 发送与接收数据

## 调查结果

- 目标文件 `02-TCP Socket/发送与接收数据.md` 目前只有 YAML、标题和空小节。
- `连接生命周期.md` 已解释连接建立、关闭、`recv() == 0`、`shutdown()` 和 Linux/Windows 关闭 API；本章重点放在数据操作本身，并用较短篇幅连接这些概念。
- `消息边界与拆包.md` 已列出定长、分隔符、长度前缀等方案；本章使用最容易观察的换行符分隔消息，并明确说明这只是示例协议。
- 仓库已有 `src/socket/server.c` 和 `client.c`，但它们是 Linux 的一次 `recv()`/`send()` 示例；本章将提供完整的 Linux 与 Windows 成对示例，不依赖修改这些现有源码。

## 内容决定

- 先解释 API 的实际语义，再给代码，避免初学者只记函数名。
- 示例使用 `127.0.0.1:8080`，只允许本机测试，降低防火墙和暴露服务的干扰。
- 示例使用 `send_all()` 处理部分发送，使用 `recv_line()` 读取到 `\\n` 才认为一条文本消息结束。
- `recv_line()` 为了教学清晰按字节接收；文档会明确指出生产代码应使用更大的接收缓冲区和累积解析，避免逐字节系统调用的性能问题。
- 每个平台都提供服务端、客户端、编译命令、运行顺序、输出示例和常见故障排查。
